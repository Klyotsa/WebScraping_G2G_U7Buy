# G2G Trello Scraper

Веб-скрапер для автоматического отслеживания заказов G2G и создания карточек в Trello с использованием Puppeteer.

## 🚀 Возможности

- **Автоматизация браузера** с Puppeteer
- **Парсинг заказов G2G** для сегментов Orders, Delivering и Boosting
- **Интеграция с Trello API** для создания карточек
- **Сохранение профиля браузера** между сессиями
- **REST API** для управления скрапером

## 📋 Установка

1. **Установите зависимости:**
```bash
npm install
```

2. **Настройте переменные окружения:**
```bash
cp env.example .env
```

3. **Отредактируйте `.env` файл:**
```env
# Trello API Configuration
TRELLO_API_KEY=your_trello_api_key_here
TRELLO_API_TOKEN=your_trello_api_token_here
TRELLO_BOARD_ID=your_board_id_here
TRELLO_LIST_ID=your_list_id_here

# Puppeteer Configuration
HEADLESS=false
USER_DATA_DIR=./user-data
```

## 🎯 Использование

### Запуск приложения:
```bash
npm start
```

### Разработка:
```bash
npm run dev
```

## 📡 API Endpoints

### Основные команды:
- `GET /` - Информация об API
- `GET /status` - Статус скрапера

### G2G Scraper:
- `POST /g2g/open-login` - Открыть страницу логина
- `POST /g2g/open-orders` - Открыть страницу заказов
- `POST /g2g/open-delivering` - Открыть страницу Delivering
- `POST /g2g/open-boosting-preparing` - Открыть страницу Boosting Preparing
- `POST /g2g/open-boosting-delivering` - Открыть страницу Boosting Delivering
- `GET /g2g/check-login` - Проверить статус логина
- `POST /g2g/scrape` - Парсить заказы
- `POST /g2g/scrape-and-create-cards` - Парсить и создать карточки Trello
- `POST /g2g/scrape-delivering` - Парсить Delivering заказы
- `POST /g2g/scrape-boosting-preparing` - Парсить Boosting Preparing заказы
- `POST /g2g/scrape-boosting-delivering` - Парсить Boosting Delivering заказы
- `POST /g2g/scrape-delivering-and-create-cards` - Парсить Delivering заказы и создать карточки
- `POST /g2g/scrape-boosting-preparing-and-create-cards` - Парсить Boosting Preparing заказы и создать карточки
- `POST /g2g/scrape-boosting-delivering-and-create-cards` - Парсить Boosting Delivering заказы и создать карточки
- `POST /g2g/scrape-all-and-create-cards` - Собрать Boosting/Delivering заказы и создать карточки
- `POST /g2g/clear-processed` - Очистить кэш обработанных заказов
- `POST /g2g/start-delivery/:orderId` - Запустить доставку для заказа
- `POST /g2g/extract-chat-data` - Извлечь данные аккаунта из чата

### Trello:
- `GET /trello/test` - Тест подключения к Trello

## 🔧 Настройка Trello API

1. **Получите API ключ:** https://trello.com/app-key
2. **Получите токен:** Нажмите на ссылку в разделе токена
3. **Получите Board ID:** Добавьте `.json` к URL доски
4. **Получите List ID:** Найдите ID списка в JSON доски

## 📝 Пример использования

```bash
# Открыть страницу логина
curl -X POST http://localhost:8080/g2g/open-login

# Проверить статус логина
curl http://localhost:8080/g2g/check-login

# Парсить заказы и создать карточки
curl -X POST http://localhost:8080/g2g/scrape-and-create-cards
```

## 🛠️ Технологии

- **Node.js** - Runtime
- **Puppeteer** - Автоматизация браузера
- **Express** - Web сервер
- **Axios** - HTTP клиент
- **dotenv** - Управление переменными окружения

## 📁 Структура проекта

```
├── src/
│   ├── scraper/
│   │   └── g2g-scraper.js    # Основной скрапер
│   └── services/
│       └── trello-service.js # Trello API интеграция
├── index.js                  # Главный файл приложения
├── package.json             # Зависимости
└── README.md               # Документация
```

## 🔒 Безопасность

- Профиль браузера сохраняется локально
- API ключи хранятся в переменных окружения
- Автоматическое закрытие браузера при завершении

## 📊 Мониторинг

Приложение логирует все операции:
- Запуск/остановка браузера
- Открытие страниц
- Парсинг данных
- Создание карточек Trello