# 🔐 Инструкция по входу в G2G на хостинге

## 📋 Процесс входа через веб-интерфейс

### Шаг 1: Откройте страницу входа

Откройте в браузере:
```
http://64.227.35.29:3000/login
```

Или если настроен домен:
```
http://your-domain.com/login
```

### Шаг 2: Введите логин и пароль

1. В поле **"E-mail или номер телефона"** введите ваш логин G2G
2. В поле **"Пароль"** введите ваш пароль
3. Нажмите кнопку **"Войти"**

### Шаг 3: Введите 2FA код (если требуется)

1. После нажатия "Войти" появится форма для ввода 2FA кода
2. Откройте приложение **Google Authenticator** (или другое приложение аутентификатора)
3. Найдите код для G2G (6 цифр)
4. Введите код в поле **"2FA Код"**
5. Нажмите кнопку **"Подтвердить"**

### Шаг 4: Проверка успешного входа

После успешного входа вы увидите сообщение:
```
✅ Вход выполнен успешно!
```

Через 2 секунды произойдет автоматическое перенаправление на главную страницу API.

## 🔧 Что происходит на сервере?

1. **Ввод логина/пароля:**
   - Данные отправляются на `/g2g/login`
   - Puppeteer открывает страницу G2G на сервере
   - Автоматически вводит логин и пароль
   - Нажимает кнопку "Login"

2. **Ввод 2FA кода:**
   - Если требуется 2FA, система ждет ввода кода
   - После получения кода через `/g2g/2fa`
   - Puppeteer заполняет 6 отдельных полей OTP
   - Автоматически нажимает кнопку "Continue"

3. **Сохранение сессии:**
   - Cookies и данные сессии сохраняются в `user-data/`
   - При следующем запуске браузер автоматически использует сохраненную сессию
   - Повторный вход не требуется до истечения сессии

## 📡 API эндпоинты для входа

### POST `/g2g/login`
Вход с логином и паролем

**Запрос:**
```json
{
  "username": "your_email@example.com",
  "password": "your_password"
}
```

**Ответ (требуется 2FA):**
```json
{
  "status": "success",
  "message": "Требуется 2FA код",
  "requires2FA": true
}
```

**Ответ (успешный вход):**
```json
{
  "status": "success",
  "message": "Вход выполнен успешно",
  "requires2FA": false
}
```

### POST `/g2g/2fa`
Ввод 2FA кода

**Запрос:**
```json
{
  "code": "123456"
}
```

**Ответ:**
```json
{
  "status": "success",
  "message": "Вход выполнен успешно"
}
```

### GET `/g2g/check-login`
Проверка статуса входа

**Ответ:**
```json
{
  "status": "success",
  "message": "Пользователь залогинен",
  "isLoggedIn": true
}
```

## 🚀 Развертывание на сервере

### 1. Загрузите обновленные файлы:

```bash
# Загрузите обновленный код
scp -r src/ root@64.227.35.29:/opt/g2g-scraper/
scp index.js root@64.227.35.29:/opt/g2g-scraper/
scp -r public/ root@64.227.35.29:/opt/g2g-scraper/

# Или используйте скрипт для автоматической загрузки (если создан)
```

### 2. Перезапустите сервис:

```bash
ssh root@64.227.35.29
cd /opt/g2g-scraper
systemctl restart g2g-scraper.service
systemctl status g2g-scraper.service
```

### 3. Проверьте доступность:

```bash
curl http://64.227.35.29:3000/login
```

## 🔒 Безопасность

⚠️ **Важно:**
- Страница входа доступна по HTTP (не HTTPS)
- Рекомендуется настроить HTTPS через nginx/apache
- Или использовать VPN для доступа к серверу
- Данные передаются в открытом виде - используйте только в защищенной сети

## 🐛 Устранение неполадок

### Проблема: Страница не открывается
- Проверьте, что сервис запущен: `systemctl status g2g-scraper.service`
- Проверьте порт: `netstat -tlnp | grep 3000`
- Проверьте логи: `tail -f /opt/g2g-scraper/logs/activity.log`

### Проблема: Ошибка входа
- Проверьте логи на сервере
- Убедитесь, что браузер запускается корректно
- Проверьте, что Chrome установлен на сервере

### Проблема: 2FA код не принимается
- Убедитесь, что код актуален (обновляется каждые 30 секунд)
- Проверьте, что все 6 полей заполнены
- Проверьте логи на сервере для деталей ошибки

## 📝 Логирование

Все действия логируются в:
```
/opt/g2g-scraper/logs/activity.log
```

Просмотр логов в реальном времени:
```bash
tail -f /opt/g2g-scraper/logs/activity.log
```



